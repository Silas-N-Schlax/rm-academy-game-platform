.container.container--xs
  .simple-page.simple-page--not-centered
    .simple-page__body
      .simple-page__logo
        img.simple-page__logo-img alt='go fish game logo' src='/logo.png'

      .simple-page__form
        = simple_form_for @user, method: :patch, html: { id: 'edit-profile-form' }, data: { controller: 'country', country_url_value: turbo_fetch_users_url } do |f|
          = f.input :name
          = f.input :email_address, input_html: { readonly: true }


          = f.input :street_address
          = f.input :country, as: :select, collection: Country.data.all,
            input_html: { data: { action: 'country#perform' } }
          #state-container
             = f.input :state, as: :select, collection: State.data.where(country_id: @user.country)

          = f.input :city
          = f.input :zip_code, as: :integer
          = content_for :modal_footer do
            = f.button :submit, "Edit Profile", form: f.id

= content_for :modal_header do
  p Edit Your Profile
